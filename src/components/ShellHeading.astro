---
interface Props {
	/** Matches the anchors Markdown headings get, so every section is linkable. */
	id?: string;
}

const { id } = Astro.props;
---

<h2 class="shell-heading" id={id}><slot /></h2>
